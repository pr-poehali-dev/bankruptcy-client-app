{
  "tests": [
    {
      "name": "OPTIONS preflight",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200,
      "bodyMatcher": "partial"
    },
    {
      "name": "GET ?action=stats без токена — 401",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 401,
      "expectedBody": {"success": false},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET ?action=clients без токена — 401",
      "method": "GET",
      "path": "/?action=clients",
      "expectedStatus": 401,
      "expectedBody": {"success": false},
      "bodyMatcher": "partial"
    }
  ]
}
