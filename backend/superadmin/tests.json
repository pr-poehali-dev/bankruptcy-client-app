{
  "tests": [
    {
      "name": "OPTIONS preflight",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200,
      "bodyMatcher": "partial"
    },
    {
      "name": "GET ?action=stats без токена — 401",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 401,
      "expectedBody": {"success": false},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET ?action=companies без токена — 401",
      "method": "GET",
      "path": "/?action=companies",
      "expectedStatus": 401,
      "expectedBody": {"success": false},
      "bodyMatcher": "partial"
    }
  ]
}
