/**
 * ======================================================
 *  БРЕНДОВЫЕ НАСТРОЙКИ — меняй только здесь
 * ======================================================
 *  Это единственный файл, который нужно редактировать
 *  чтобы адаптировать приложение под конкретную компанию.
 */

export const BRAND = {
  // ── Название и описание ──────────────────────────────
  appName: "Клиентский портал",
  companyName: "Юридическая компания",
  tagline: "Юридическое сопровождение",

  // ── Логотип ──────────────────────────────────────────
  // Укажи URL изображения или оставь пустым — тогда будет иконка
  logoUrl: "",
  // Иконка lucide когда logoUrl пустой
  logoIcon: "Scale",

  // ── Реферальная программа ────────────────────────────
  referral: {
    bonusAmount: 10000,          // Бонус за 1 реферала в ₽
    bonusLabel: "10 000 ₽",     // Текст суммы для отображения
    promoPrefix: "REF",          // Префикс промокода (REFIVA2025)
  },

  // ── Контакты поддержки ───────────────────────────────
  supportPhone: "+7 (800) 000-00-00",
  supportEmail: "",

  // ── Цвета (hex) ──────────────────────────────────────
  // Меняются CSS-переменные в index.css — primary и accent
  colors: {
    primary: "#00a8cc",    // Основной акцент (синий)
    accent: "#a855f7",     // Вторичный акцент (фиолетовый)
  },
} as const;

export type BrandConfig = typeof BRAND;
